const state = {
    maxBrojeva : 48
}

const getters = {
    get_maxBrojeva : state => state.maxBrojeva
}

const actions ={};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
}